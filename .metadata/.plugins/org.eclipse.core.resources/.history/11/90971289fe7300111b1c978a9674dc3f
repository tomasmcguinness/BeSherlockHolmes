<h1>Become Sherlock Holmes</h1>
<%= form_for(@post) do |f| %>

<% if @post.errors.any? %>
<div id="errorExplanation">
	<h2><%= pluralize(@post.errors.count, "error") %> prohibited
	this post from being saved:</h2>
	<ul>
		<% @post.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>
<p>
	Create a new geo trigger
</p>
<button type="submit">
	Create Place
</button>
<%end%> 